spring.application.name=backend

server.port=8080

# Temporary in-memory storage - no database required
# Data is lost on application restart
app.storage.type=in-memory

logging.level.com.prueba.backend=DEBUG
